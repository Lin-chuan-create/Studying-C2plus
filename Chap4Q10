//编写一个程序，让用户输入三次40码跑的成绩（如果您愿意，也可让用户输入40米跑的成绩），并显示次数和平均成绩。请使用一个array对象来存储数据（如果编译器不支持array类，请使用数组）。
#include <iostream>
#include<array>
using namespace std;

int main()
{
	cout << "请输入三次四十米跑的成绩\n";
	array<double, 3>grade;
	cin >> grade[0] >> grade[1] >> grade[2];
	cout << "次数：3\n";
	cout << "平均成绩：" << (grade[0] + grade[1] + grade[2]) / 3;
	return 0;
}
